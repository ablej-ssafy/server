jwt:
  secret: asdqwfjidjogwieogkdnvoiweogiweywhrudjbf
  access-expire: 1000000000
  refresh-expire: 1000000000

app:
  client-base-url: http://localhost:3000
  server-base-url: http://localhost:8080
#  ai-base-url: http://localhost:5000

minio:
  endpoint: http://127.0.0.1:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket:
    images: images       # Public 버킷
    resume: resume       # Private 버킷

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ablej
    username: ablej
    password: ablej

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  data:
    redis:
      host: localhost
      port: 6379
      password: ablej
      repositories:
        enabled: false

# Mattermost 설정
notification:
  mattermost:
    enabled: true  # mmSender를 사용할 지 여부, false면 알림이 오지 않는다
    webhook-url: https://meeting.ssafy.com/hooks/89k6cs8ak78mdxn1yh363ckemr  # 위의 Webhook URL을 기입
    channel:   # 기본 설정한 채널이 아닌 다른 채널로 보내고 싶을 때 기입한다
    pretext: ""  # attachments의 상단에 나오게 되는 일반 텍스트 문자
    color: ""  # attachment에 왼쪽 사이드 컬러. default=red
    author-name: ""  # attachment의 상단에 나오는 이름
    author-icon: ""  # author-icon 왼쪽에 나올 아이콘의 url링크
    footer: ""  # attachment에 하단에 나올 부분. default=현재 시간
